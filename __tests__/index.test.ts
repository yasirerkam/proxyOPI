import { describe, expect, test } from '@jest/globals';
import { ProxyOPI, Proxy, ProxyList, Protocol } from "../src/index.ts";

describe('sum module', () => {
    test('testtt', () => {


        // expect(xyz(a, b)).toBe(3);
    });
});